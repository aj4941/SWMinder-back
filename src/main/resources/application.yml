spring:
  jpa:
    hibernate:
      ddl-auto: create # TODO: change to validate
    database: mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/swminder # TODO: future, jasypt...
    username: root
    password:
